Delete Bookmark
---
tags:
  - Bookmarks
parameters:
  - name: id
    description: bookmark id
    in: path
    required: true
    schema:
      type: integer
      required:
        - "id"
responses:
  200:
    description: When a user successfully delete bookmark

  400:
    description: Fails to delete bookmark due to bad request data
